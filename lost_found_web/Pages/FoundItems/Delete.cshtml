@page "{id:int}"
@model lost_found_web.Pages.FoundItems.DeleteModel
@{
    ViewData["Title"] = "Delete Found Item";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0"><i class="fas fa-trash"></i> Delete Found Item</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle"></i>
                        <strong>Warning!</strong> Are you sure you want to delete this found item? This action cannot be undone.
                    </div>

                    <div class="card mb-3">
                        <div class="card-body">
                            <h5 class="card-title text-success">@Model.FoundItem.ItemName</h5>
                            <p class="card-text">
                                <strong>Description:</strong> @Model.FoundItem.Description<br>
                                <strong>Found Location:</strong> @Model.FoundItem.FoundLocation<br>
                                <strong>Found Date:</strong> @Model.FoundItem.FoundDate.ToString("MMM dd, yyyy")<br>
                                <strong>Contact:</strong> @Model.FoundItem.ContactInfo
                            </p>
                            <small class="text-muted">
                                Reported: @Model.FoundItem.CreatedAt.ToString("MMM dd, yyyy 'at' h:mm tt")
                            </small>
                        </div>
                    </div>

                    <form method="post">
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-page="Index" class="btn btn-secondary me-md-2">Cancel</a>
                            <button type="submit" class="btn btn-danger">
                                <i class="fas fa-trash"></i> Delete Found Item
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
