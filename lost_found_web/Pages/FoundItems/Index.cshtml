@page
@model lost_found_web.Pages.FoundItems.IndexModel
@{
    ViewData["Title"] = "Found Items";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2><i class="fas fa-hand-holding"></i> Found Items</h2>
                <a asp-page="Create" class="btn btn-success">
                    <i class="fas fa-plus"></i> Report Found Item
                </a>
            </div>

            @if (Model.FoundItems.Any())
            {
                <div class="row">
                    @foreach (var item in Model.FoundItems)
                    {
                        <div class="col-md-6 col-lg-4 mb-4">
                            <div class="card h-100 shadow-sm">
                                <div class="card-body">
                                    <h5 class="card-title text-success">@item.ItemName</h5>
                                    <p class="card-text">
                                        <strong>Description:</strong> @item.Description<br>
                                        <strong>Found Location:</strong> @item.FoundLocation<br>
                                        <strong>Found Date:</strong> @item.FoundDate.ToString("MMM dd, yyyy")<br>
                                        <strong>Contact:</strong> @item.ContactInfo
                                    </p>
                                </div>
                                <div class="card-footer bg-light">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <small class="text-muted">
                                            Reported: @item.CreatedAt.ToString("MMM dd, yyyy 'at' h:mm tt")
                                        </small>
                                        <div class="btn-group" role="group">
                                            <a asp-page="./Edit" asp-route-id="@item.Id" class="btn btn-outline-success btn-sm">
                                                <i class="fas fa-edit"></i> Edit
                                            </a>
                                            <a asp-page="./Delete" asp-route-id="@item.Id" class="btn btn-outline-danger btn-sm">
                                                <i class="fas fa-trash"></i> Delete
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <i class="fas fa-hand-holding fa-3x text-muted mb-3"></i>
                    <h4 class="text-muted">No found items reported</h4>
                    <p class="text-muted">Be the first to report a found item!</p>
                    <a asp-page="Create" class="btn btn-success">
                        <i class="fas fa-plus"></i> Report Found Item
                    </a>
                </div>
            }
        </div>
    </div>
</div>
